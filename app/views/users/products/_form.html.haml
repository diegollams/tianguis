= simple_form_for([@user, @product]) do |f|
  = f.error_notification
  = f.error :base

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :price
    = f.input :image_url

  .form-actions
    = f.button :submit
