= simple_form_for([@user, @product]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description
    = f.input :price
    = f.input :image_url

  .form-actions
    = f.button :submit
